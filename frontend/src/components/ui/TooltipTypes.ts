// RESPONSIBILITY: Type contract extracted from Tooltip.tsx; no business behavior.
import type { ReactNode } from 'react';

export interface TooltipProps {
    content: string;
    children: ReactNode;
}
