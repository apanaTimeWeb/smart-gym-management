// RESPONSIBILITY: Pure Server Component for the migrations page. Renders the interactive client component.
import MigrationsClient from '@/app/superadmin/migrations/MigrationsClient';

export default function Page() {
  return <MigrationsClient />;
}
