// RESPONSIBILITY: Lets a Superadmin select an audience insight and exposes the selected audience for the downstream broadcast workflow.
'use client';
import { useState } from 'react';
import { CheckCircle2 } from 'lucide-react';
import { formatNumber } from '@/lib/formatters';
import Panel from '@/components/ui/Panel';
import type { SuperadminBroadcastsV1SectionProps } from '@/app/superadmin/broadcasts/broadcasts_types/SuperadminBroadcastsV1Types';
export default function SuperadminBroadcastsV1AudienceBuilderPanel({ data }: SuperadminBroadcastsV1SectionProps) { const [selected,setSelected]=useState(data.segments[0]?.name??''); return <Panel title="Audience builder" description="Start with a saved signal and refine the audience before sending."><div className="grid grid-cols-1 gap-3 md:grid-cols-2 xl:grid-cols-5">{data.segments.map(s=><button key={s.name} type="button" aria-pressed={selected===s.name} onClick={()=>setSelected(s.name)} className={`rounded-lg border p-4 text-left focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary ${selected===s.name?'border-primary bg-primary-subtle':'border-border bg-input'}`}><div className="flex items-start justify-between gap-2"><span className="truncate font-medium text-primary">{s.name}</span>{selected===s.name?<CheckCircle2 size={18} className="text-primary"/>:<span className="rounded-full bg-primary-subtle px-2 py-1 text-xs font-semibold text-primary">{formatNumber(s.count)}</span>}</div><p className="mt-2 text-xs text-secondary">{s.description}</p></button>)}</div>{selected?<p className="mt-4 rounded-md border border-primary/30 bg-primary-subtle p-3 text-sm text-primary" role="status">Selected audience: {selected}</p>:null}</Panel>; }
