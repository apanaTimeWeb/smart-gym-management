// RESPONSIBILITY: URL contracts owned exclusively by the Trainer Profile feature.
export const ProfileUrlConfig = {
  PAGES: { LIST: '/trainer/profile' },
  BACKEND_API: { PROFILE: '/trainer/profile', PASSWORD: '/trainer/profile/password' },
} as const;
