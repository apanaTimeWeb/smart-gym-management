import { z } from 'zod';

const emergencyContactSchema = z.object({ name: z.string(), phone: z.string(), relation: z.string() }).optional();
const documentSchema = z.object({ type: z.string(), url: z.string(), uploadedAt: z.string() }).optional();
export const staffSchema = z.object({ id: z.string(), name: z.string(), email: z.string(), phone: z.string(), role: z.string(), salary: z.number(), branch: z.string(), gender: z.string(), address: z.string().optional(), aadhaar: z.string().optional(), upiId: z.string().optional(), advanceSalary: z.number().optional(), joinDate: z.string(), isActive: z.boolean(), salaryType: z.enum(['Monthly', 'Daily']).optional(), paymentCycle: z.enum(['Monthly', 'Bi-Weekly', 'Weekly']).optional(), currentDue: z.number().optional(), bankAccountNumber: z.string().optional(), ifscCode: z.string().optional(), panNumber: z.string().optional(), department: z.string().optional(), emergencyContact: emergencyContactSchema, documents: z.array(documentSchema).optional(), });
export const payrollSchema = z.object({ id: z.string(), staffId: z.string(), month: z.string(), amount: z.number(), netPayable: z.number(), paidAmount: z.number(), pendingAmount: z.number(), status: z.string(), paidAt: z.string().optional(), notes: z.string().optional(), deductions: z.object({ tds: z.number(), pf: z.number(), esi: z.number(), other: z.number() }), staff: z.object({ name: z.string(), role: z.string() }).optional(), });
export const hrSummarySchema = z.object({ totalSalaryThisMonth: z.number(), totalSalaryPaid: z.number(), totalSalaryDue: z.number(), totalAdvanceGiven: z.number(), pendingPaymentsCount: z.number(), totalStaff: z.number(), activeStaff: z.number(), totalPayrollThisMonth: z.number(), paidCount: z.number() });
export const ledgerEntrySchema = z.object({ id: z.string(), staffId: z.string(), date: z.string(), type: z.enum(['Salary Generated', 'Salary Paid', 'Advance Given', 'Due Paid']), credit: z.number(), debit: z.number(), balance: z.number(), notes: z.string().optional(), referenceNo: z.string().optional(), paymentMode: z.string().optional(), });
