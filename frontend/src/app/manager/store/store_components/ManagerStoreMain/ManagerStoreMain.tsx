'use client';
// RESPONSIBILITY: Entry component for the Store module. Wraps the UI in the hook-based state facade and handles page layout.
import { ManagerStoreContent } from '@/app/manager/store/store_components/ManagerStoreMain/ManagerStoreContent/ManagerStoreContent';

export default function ManagerStoreMain() {
  return <ManagerStoreContent />;
}
