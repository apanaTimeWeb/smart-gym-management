// RESPONSIBILITY: Type definitions for the owning Manager UI component.

export interface ManagerPaginationProps {
  currentPage: number;
  totalPages: number;
  onPageChange: (page: number) => void;
  totalItems?: number;
  itemsPerPage?: number;
}
