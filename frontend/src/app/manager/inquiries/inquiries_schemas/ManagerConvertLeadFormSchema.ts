// RESPONSIBILITY: Defines validation rules for converting an inquiry into a member admission.
import { z } from 'zod';
import { MANAGER_INQUIRY_MAX_AMOUNT_MAJOR_UNITS, MANAGER_INQUIRY_MAX_CUSTOM_DAYS } from '@/app/manager/inquiries/inquiries_utils/ManagerInquiriesConvertConstants';

export const INQUIRIES_BILLING_CYCLE_VALUES = ['ONE_MONTH', 'THREE_MONTHS', 'SIX_MONTHS', 'TWELVE_MONTHS', 'CUSTOM'] as const;
export const managerConvertLeadFormSchema = z.object({ name: z.string().min(2, 'Name is required'), email: z.string().email('Invalid email').optional().or(z.literal('')), phone: z.string().min(10, 'Valid phone is required'), address: z.string().optional(), aadhaar: z.string().optional(), gender: z.enum(['MALE', 'FEMALE', 'OTHER']), billingCycle: z.enum(INQUIRIES_BILLING_CYCLE_VALUES), customDays: z.number().min(1).max(MANAGER_INQUIRY_MAX_CUSTOM_DAYS).optional(), planId: z.string().min(1, 'Plan is required'), joinDate: z.string().min(1, 'Join date is required'), expiryDate: z.string().optional(), totalAmount: z.number().min(0).max(MANAGER_INQUIRY_MAX_AMOUNT_MAJOR_UNITS).optional(), paidAmount: z.number().min(0).max(MANAGER_INQUIRY_MAX_AMOUNT_MAJOR_UNITS).optional(), pendingAmount: z.number().min(0).max(MANAGER_INQUIRY_MAX_AMOUNT_MAJOR_UNITS).optional(), medicalHistory: z.string().optional() });
