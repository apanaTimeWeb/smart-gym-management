// RESPONSIBILITY: URL configuration for the Admin Reports module
export const AdminReportsUrlConfig = {
  root: '/admin/reports',
  api: {
    branchReference: '/admin/branches/fetchBranches',
    base: '/admin/reports',
  }
};
