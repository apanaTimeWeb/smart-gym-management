/**
 * Centralized URL configuration for the admin_e2e module.
 * No URLs should be hardcoded directly in components or API services.
 */

export const ADMIN_ADMIN_E2E_URLS = {
    PAGES: {
        ROOT: '/admin/admin_e2e',
    },
    API: {
        BASE: '/admin/admin_e2e',
    }
} as const;
