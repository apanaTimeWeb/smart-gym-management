// RESPONSIBILITY: URL configuration for the Admin Notifications module
export const AdminNotificationsUrlConfig = {
  root: '/admin/notifications',
  api: {
    base: '/api/admin/notifications',
  }
};
