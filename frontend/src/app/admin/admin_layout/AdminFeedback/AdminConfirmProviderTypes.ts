// RESPONSIBILITY: Defines the children contract for the global zero-business confirmation provider.
import type { ReactNode } from 'react';

export interface AdminConfirmProviderProps {
  children: ReactNode;
}
