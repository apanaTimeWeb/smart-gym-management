// RESPONSIBILITY: Re-exports feature enum vocabulary for dashboard without a barrel file.
// FLOW: DTO/entity -> enum constant.
export const DashboardSortFields = ['createdAt', 'updatedAt'] as const;
