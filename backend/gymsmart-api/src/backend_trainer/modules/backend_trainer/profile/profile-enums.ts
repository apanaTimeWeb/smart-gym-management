// RESPONSIBILITY: Defines finite profile domain enum values used by DTOs and persistence.
// FLOW: profile DTO/entity → typed enum → API/DB contract.

export enum TrainerProfileRole { TRAINER = 'TRAINER' }
