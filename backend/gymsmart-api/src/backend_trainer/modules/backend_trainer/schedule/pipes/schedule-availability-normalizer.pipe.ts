// RESPONSIBILITY: Normalizes the frontend raw array availability payload into the DTO wrapper expected by validation.
// FLOW: Raw JSON array/object → normalized ScheduleUpdateAvailabilityBodyDto.

import { ArgumentMetadata, Injectable, PipeTransform, BadRequestException } from '@nestjs/common'; import { plainToInstance } from 'class-transformer'; import { validateSync } from 'class-validator'; import { ScheduleUpdateAvailabilityBodyDto } from '@/backend_trainer/modules/backend_trainer/schedule/dtos/schedule-update-availability-body.dto'; @Injectable() export class ScheduleAvailabilityNormalizerPipe implements PipeTransform { /** Normalizes both documented object and actual frontend raw-array payload forms. */ transform(value:unknown,_metadata:ArgumentMetadata):ScheduleUpdateAvailabilityBodyDto{const source=Array.isArray(value)?{days:value}:value; const dto=plainToInstance(ScheduleUpdateAvailabilityBodyDto,source); const errors=validateSync(dto); if(errors.length) throw new BadRequestException(errors); return dto;} }
