// RESPONSIBILITY: Replaces the trainer weekly availability inside one tenant transaction.
// FLOW: Schedule command → availability service → UnitOfWork → repository.

import { Injectable } from '@nestjs/common'; import { CoreRequestContext } from '@/backend_trainer/core/context/core-request-context'; import { CoreUnitOfWorkService } from '@/backend_trainer/core/database/core-unit-of-work.service'; import { CoreAuditService } from '@/backend_trainer/core/audit/core-audit.service'; import { ScheduleRepository } from '@/backend_trainer/modules/backend_trainer/schedule/repositories/schedule-repository';
import { ScheduleAvailabilityMapper } from '@/backend_trainer/modules/backend_trainer/schedule/schedule-availability.mapper';
import type { ScheduleAvailabilityDomain } from '@/backend_trainer/modules/backend_trainer/schedule/schedule-availability.domain'; import { ScheduleUpdateAvailabilityBodyDto } from '@/backend_trainer/modules/backend_trainer/schedule/dtos/schedule-update-availability-body.dto'; @Injectable() export class ScheduleAvailabilityUpdateService { constructor(private readonly uow:CoreUnitOfWorkService,private readonly repo:ScheduleRepository,private readonly audit:CoreAuditService){} /** Replaces seven availability rows and records the mutation. */ async update(dto:ScheduleUpdateAvailabilityBodyDto):Promise<ScheduleAvailabilityDomain[]>{const id=CoreRequestContext.get().userId??''; const days=dto.days.map((day)=>({day:day.day,isAvailable:day.isAvailable,startTime:day.startTime,endTime:day.endTime})); const result=await this.uow.execute((context)=>this.repo.replaceAvailability(id,days,context)); await this.audit.record('TRAINER_AVAILABILITY_UPDATED','WEEKLY_AVAILABILITY',id,null,{daysCount:days.length}); return result.map(ScheduleAvailabilityMapper);} }
