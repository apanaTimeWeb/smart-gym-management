// RESPONSIBILITY: Reads diet plans and assigned-member data within the Library feature boundary.
// FLOW: Library query controller → query service → LibraryDietPlanRepository.

import { Injectable } from '@nestjs/common'; import { CoreRequestContext } from '@/backend_trainer/core/context/core-request-context'; import { LibraryDietPlanRepository } from '@/backend_trainer/modules/backend_trainer/library/repositories/library-diet-plan.repository'; import { buildCorePaginationMeta } from '@/backend_trainer/core/utils/core-pagination.utils'; import { LibraryDietPlanMapper } from '@/backend_trainer/modules/backend_trainer/library/library-diet-plan.mapper'; @Injectable() export class LibraryQueryService { constructor(private readonly repo:LibraryDietPlanRepository){} /** Returns paginated diet plans for trainer lookup. */ async findDietPlans(query:{page:number;limit:number;search?:string;goal?:string;sortBy:string;sortDirection:string}):Promise<{dietPlans:ReturnType<typeof LibraryDietPlanMapper>[];total:number;page:number;limit:number;pagination:ReturnType<typeof buildCorePaginationMeta>}>{const result=await this.repo.findDietPlans(query); return {dietPlans:result.rows.map(LibraryDietPlanMapper),total:result.total,page:query.page,limit:query.limit,pagination:buildCorePaginationMeta(result.total,query.page,query.limit)};} /** Returns member assignment rows. */ async findAssignedMembers():Promise<Array<Record<string,unknown>>>{return this.repo.findAssignedMembers(CoreRequestContext.get().userId ?? '');} }
