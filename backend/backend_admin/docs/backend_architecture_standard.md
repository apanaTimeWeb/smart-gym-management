# Backend Architecture Standard

This package follows the supplied `backend_development_instruction.md` as its normative architecture source.

Implementation choice for this project: NestJS + TypeScript + PostgreSQL + TypeORM + Redis.

The Feature Module is the default AI repair/write boundary. Frontend feature names are canonical for backend module names and route semantics.

For audit discipline, see `frontend_backend_audit_standard.md`.
