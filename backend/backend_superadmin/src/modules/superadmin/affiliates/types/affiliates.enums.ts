// RESPONSIBILITY: Re-exports feature enum vocabulary for affiliates without a barrel file.
// FLOW: DTO/entity -> enum constant.
export const AffiliatesSortFields = ['createdAt', 'updatedAt'] as const;
